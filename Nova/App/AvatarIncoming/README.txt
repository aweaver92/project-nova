Drop avatar PNGs here with these exact names, then tell the agent they're ready:

  Claude.png  -> AvatarClaude
  Sage.png    -> AvatarSage
  Ivy.png     -> AvatarIvy

Square PNG (~512x512 or larger) works best. White background is fine.
